.fonts.confについて

概要
  2011年 4月現在 64bit環境では Acroreadが提供されてないので,
  evinceで見る必要があるが, 適切な設定をしないと全く読めない.

必要なパッケージのインストール
  poppler-dataが必要なのでインストールする.

  % sudo aptitude install poppler-data

.fonts.confについて
  同ディレクトリの fonts.confを参照のこと.

